# Industrial AI Process Logic Report

**Submission repository:** [https://github.com/j-avdeev/industrial-ai-process-logic](https://github.com/j-avdeev/industrial-ai-process-logic)

**Current submission status:** mature as a runnable, auditable pipeline and Leonardo handoff; not a final scored model claim until official eval files are staged, the Leonardo 50k-150k-per-family generation/training run completes, and the returned package verifier reports `final_leonardo_objective_ready=true`.

## TL;DR

This repository implements a reproducible Industrial AI pipeline for semiconductor process sequences. It combines a grammar-aware baseline, official validator-based anomaly detection, optional step-token transformer training, and exact submission CSV generation for the three Track 1 tasks.

## Problem

The challenge asks whether models can learn process logic in long semiconductor fabrication routes rather than only memorizing common step strings. The scored tasks are next-step prediction, sequence completion, and anomaly detection.

## Approach

- Treat each process step string as one token.
- Condition predictions on product family: `MOSFET`, `IGBT`, or `IC`.
- Use the official process grammar and validator as a strong reliability layer.
- Use n-gram and nearest-prefix baselines for immediate reproducible predictions.
- Use deterministic validator output for anomaly detection and rule attribution.
- Improve next-step and completion tasks as candidate generation plus reranking: retrieval from a larger generated corpus, n-gram beam fallback for completion, official validation, length penalties, and optional transformer checkpoint scoring.
- Provide optional PyTorch transformer training for scaling experiments across `tiny`, `small`, and `medium` configurations.

## Current Results

The repo is ready to run local synthetic dev evaluation:

```bash
python -m industrial_ai.prepare
python -m industrial_ai.make_devset --valid-per-family 5 --anomaly-valid-per-family 5 --anomaly-invalid-per-family 5
python -m industrial_ai.infer --completion-mode ensemble
python -m industrial_ai.metrics
python -m industrial_ai.compare_completion
```

Official leaderboard metrics require the organizer-provided eval files and ground-truth scoring script. The final CSV outputs are produced under `submissions/`.

Important current caveat: completion exact match is still `0.0000` on the tiny synthetic smoke sets. This is expected for the smoke stage and does not indicate a broken code path. The improvement path is to generate a larger corpus, ideally 50k-150k sequences per family on Leonardo, train `tiny`, `small`, and `medium`, then use the checkpoint reranker selected by `industrial_ai.compare_rerankers`.

Corpus audits write `artifacts/corpus_audit/files.csv`, `families.csv`, and `summary.json`, including a generated-sequences-per-family threshold check and corpus fingerprint. When generated CSVs are present, the audit checks generated sidecar metadata for a successful generation origin, matching family, matching sequence count, and matching output SHA-256. The prepared vocabulary/stats path uses the same generated-aware corpus loader as training, inference, and comparisons; Leonardo generation/full-pipeline scripts run it after the generated-corpus audit. Training runs persist `train_summary.json`, `train_log.json`, and optional `loss_curve.png` next to each checkpoint. `train_summary.json` records hashes for both `model.pt` and `train_log.json`, and checkpoint reuse/validation/package/final-audit checks reject stale or swapped files. Completion comparisons write `artifacts/completion_compare/metrics.csv` and `metrics.json`; reranker comparisons write `artifacts/reranker_compare/metrics.csv`, `family_metrics.csv`, `metrics.json`, `REPORT.md`, and optional plots, so Leonardo scaling runs leave enough evidence to select a checkpoint for next-step and completion reranking. Leonardo finalization requires the medium completion-comparison checkpoint scorer and all requested checkpoint rerankers to load, then runs reranker comparison with `selection_scope=checkpoints` and strict selected-checkpoint failure, keeping baseline as a comparator while selecting the best eligible checkpoint for inference.

Preflight checks write `artifacts/preflight*.json` and fail early on missing raw data, missing eval files when required, malformed eval headers, empty or zero-row eval CSVs, missing PyTorch, or unavailable CUDA when GPU training is requested. `industrial_ai.stage_eval_inputs` provides a checked staging step for organizer CSVs, copying them into `data/eval/` and writing `artifacts/eval_staging_manifest.json` with source/staged row counts and SHA-256 hashes; job-time readiness requires that manifest to match the current staged eval files when eval is required, and preflight/readiness/final audit/evidence report all compare the same eval hashes. Generated-corpus metadata records the generated CSV SHA-256, and corpus audit rejects metadata sidecars that no longer match the current generated file bytes. CUDA preflight evidence records device count, GPU names, compute capability, memory, CUDA runtime version, and cuDNN version. The Leonardo full pipeline carries those checks into final validation, and strict final audit checks the packaged preflight JSON directly, so the final package cannot be produced from a run whose validation summary or package evidence did not require PyTorch, CUDA, and schema-valid eval preflight evidence.

Generated corpus files are written atomically in chunks, deduplicated across chunks, and can be reused with `--skip-if-complete`, which makes long cluster jobs retryable without silently training on partial CSV output. Reuse requires a successful prior generation metadata chain, so a bare CSV cannot be promoted into a valid generated corpus by retrying with `--skip-if-complete`. Leonardo generation also uses `--exact-count`, so stale larger outputs fail immediately instead of being relabeled as a successful reuse. Generation metadata records duplicate counts and unique-sequence shortfalls, and partial temp CSVs are removed on shortfall. Leonardo launch guards now require final `COUNT_PER_FAMILY` to stay in the 50k-150k objective range, and audits use the requested count as both the generated-corpus minimum and maximum, preventing a stale larger corpus from changing the intended 50k or 150k training run.

Training runs also support `--skip-if-complete`; existing checkpoints are reused only when the saved summary matches the requested model size, hyperparameters, seed, corpus size, per-family counts, corpus fingerprint, and active source-bundle SHA when source-bundle proof is enabled. Leonardo GPU scripts audit the requested generated-corpus threshold before training and pass `--require-device`, so requested CUDA training cannot silently fall back to CPU.
`industrial_ai.checkpoint_audit` now runs immediately after Leonardo scaling training and at finalization start, before dev comparisons or inference. It checks that `tiny`, `small`, and `medium` checkpoint summaries/logs exist, were trained for the requested epochs on CUDA, match the audited corpus totals and fingerprint, still hash-match their model files, and record the same derived run profile as validation.

Final inference reads the reranker comparison artifact by default. Exploratory/local inference can skip checkpoint scoring if baseline wins or a checkpoint is unavailable during comparison. Strict Leonardo validation and packaging now require checkpoint-only reranker selection plus a selected checkpoint, so a final run cannot silently pass with baseline selected.
The standalone and final Leonardo inference paths are strict as well: they request or use a GPU, default transformer scoring to CUDA, fail if the selected/default checkpoint scorer cannot load, and require the inference checkpoint to match the selected reranker checkpoint.
Each inference run writes `inference_summary.json` next to the submission CSVs so validation can prove row counts, corpus fingerprint, checkpoint hash, and checkpoint usage for the packaged files.
Validation compares this summary against reranker comparison output, preventing a package from mixing a selected checkpoint with submissions produced by a different inference path.

The full Leonardo pipeline ends with `industrial_ai.validate_run`, which fails the job if preflight evidence, corpus thresholds, checkpoint summaries, exact checkpoint training counts and fingerprints against the audited corpus, checkpoint model hash consistency, CUDA checkpoint device evidence, CUDA transformer-scorer device evidence for reranker comparison, completion comparison, and inference, selected-checkpoint reranker evidence, required checkpoint-size reranker rows, required completion-comparison checkpoint size, selected reranker score ordering, minimum checkpoint epoch counts, exact reranker/completion comparison corpus sizes and fingerprints against the audit, exact inference corpus size and fingerprint against the audit, checkpoint loading during reranker comparison, reranker/completion comparison example counts, reranker selection, or required submission files are missing.
It writes `artifacts/validation_summary.json` so the final package records the validation thresholds, derived run profile, and pass/fail evidence.

Leonardo scripts write the latest stage to `artifacts/run_manifest.json` and append every stage to `artifacts/run_manifest_events.jsonl` to preserve run parameters, the completion-comparison checkpoint (`checkpoints/medium/model.pt` for the full Leonardo run), derived run profile (`standard`, `max`, or `custom`), git state, Python/platform details, Slurm metadata, and links to the main artifacts, including shell-audit, readiness, checkpoint audit, validation, package manifest, and package checksum sidecar. After readiness exists, later manifest stages also copy the verified source-bundle evidence from readiness, including bundle SHA-256, manifest source, verified state, and source-bundle requirement flag. Validation summaries, checkpoint audits, package manifests, run manifests, and terminal manifest events all carry the same derived run profile, and strict packaging/final audit reject mismatches. Full, split-generation, and finalizer runs record `generation_prepared` after exact generated-corpus audit; full and finalizer runs append `checkpoint_audited` after checkpoint audit and `comparisons_complete` after medium completion comparison plus checkpoint-only reranker selection. Strict packaging/final audit require `generation_prepared`, `checkpoint_audited`, `comparisons_complete`, and the terminal packaged stage in order, with post-readiness stages matching the packaged readiness source-bundle hash when that proof is required. The full pipeline records `validated_with_submissions` before packaging, then `packaged_with_submissions` after the ZIP is written; the final strict package requires that terminal stage in both manifest files. Without eval inputs it records `validated_without_submissions`.
If official eval inputs are expected, the full pipeline can be launched with `REQUIRE_EVAL=1` or explicit eval paths; that makes missing eval files fail during preflight instead of silently producing only the no-submission validation branch.
Use `industrial_ai.leonardo_readiness --count-profile standard` for the 50k/family launch target or `--count-profile max` for the 150k/family upper target; direct `--count-per-family 50000` and `--count-per-family 150000` normalize to the same named profiles, while other direct overrides remain available for custom dry runs. When eval inputs are supplied or required, readiness validates the required columns and row counts before printing launch commands.
Readiness now records launch commands, dependency-safe split-job commands, resume guidance, returned-package verification commands, eval staging manifest status, and optional source-bundle checksum/manifest evidence in its JSON output, so the staged-run artifact captures the exact command set needed to submit, safely queue or resume, and audit the package after it is copied back. It also writes `artifacts/leonardo_launch_commands.sh`; strict packaging includes that generated command script, and final audit checks it for probe, full-pipeline, dependency-safe split, returned-package verification, and evidence-report commands. A pre-upload-only `--defer-eval-staging` mode can generate handoff commands that target `data/eval/` before the official eval CSVs are available locally, but job-time readiness without that flag requires `artifacts/eval_staging_manifest.json` to match the current eval CSV hashes, and final packaging/final audit/evidence reporting reject readiness evidence that still has deferred eval staging enabled. Stale source-bundle evidence is a warning by default; with `--require-source-bundle`, readiness requires the ZIP, sidecar, manifest, and current tree to agree before upload, generated full/finalizer commands export `REQUIRE_SOURCE_BUNDLE=1`, add explicit root-level bundle paths when the staged bundle is not under `artifacts/`, job-time readiness requires the same source-bundle proof, and final audit checks the packaged launch commands for that export. The recorded verification commands also carry the source-bundle proof self-test, explicit returned-package source-bundle proof, and package-evidence precedence flags. It also checks that final scripts use the `medium` checkpoint for completion comparison before package/final audit later prove that evidence. Generated Slurm commands use `--export=ALL,...` to preserve the submitted environment while setting run parameters.
`industrial_ai.leonardo_bundle` creates a source ZIP plus checksum and manifest for manual staging on Leonardo. It includes source code, Slurm scripts, raw training data, placeholders, and documentation, and excludes local artifacts, checkpoints, submissions, virtual environments, and `.env`; official eval CSVs can be included explicitly with `--include-eval`. The external and ZIP-embedded manifests include a handoff section with upload files, bundle/root verification commands, source-bundle readiness commands, source-bundle self-test command, and handoff audit command, and strict readiness requires that handoff metadata when source-bundle proof is requested. The same module verifies the uploaded ZIP using the embedded manifest, then verifies the unpacked source tree against the manifest before jobs are submitted.
`industrial_ai.leonardo_handoff` audits the local pre-upload handoff as one unit: source bundle ZIP, sidecar, manifest, readiness JSON, generated launch commands, source-bundle self-test evidence, and bundle SHA agreement between readiness and the ZIP.
The readiness output includes `scripts/leonardo_probe.sh`, a small GPU smoke job that writes to `artifacts/probe/` and verifies CUDA preflight, one `tiny` CUDA training epoch, checkpoint completion/reranker scoring, selected-checkpoint inference, and strict validation before the full corpus job is submitted.
`industrial_ai.leonardo_shell_audit` provides a local, Bash-free shell-script sanity check for the Leonardo launch scripts; readiness invokes it and fails on missing shebangs, CRLF line endings, missing strict mode, missing launch guards, missing Slurm log setup, or common unbalanced quote/bracket mistakes. It records byte counts and SHA-256 hashes for each Leonardo script, and final audit requires matching script copies under package evidence.
The Slurm scripts source `scripts/leonardo_common.sh` and fail before preflight/generation/training if final-run launch settings are out of range or invalid, including generated counts outside 50k-150k per family, epochs below 6, reranker validation sets below 40 examples per family, invalid model sizes, invalid batch sizes, or non-CUDA Leonardo inference.
For split Leonardo jobs, `scripts/leonardo_finalize.sh` runs the same strict post-training path after generation and `tiny`/`small`/`medium` training: reranker comparison, selected-checkpoint inference, validation, terminal manifest packaging, package verification, and final audit.

When official eval files are present, the full pipeline packages `nextstep.csv`, `completion.csv`, `anomaly.csv`, and evidence files into `artifacts/submission_package/track1_submission.zip` with a hash manifest and a `track1_submission.zip.sha256` transfer checksum. Evidence includes Leonardo shell scripts, shell-audit output, readiness output, source-bundle proof self-test output, preflight output, eval staging manifest, run manifests, validation summary, corpus audits, generated CSV metadata sidecars, reranker metrics, and checkpoint training summaries/logs. When readiness required source-bundle proof, strict packaging fails before writing the ZIP unless the ordered `generation_prepared`, `checkpoint_audited`, `comparisons_complete`, and terminal events carry the same bundle SHA as readiness, the source-bundle proof self-test passed, and the package manifest records that source-bundle summary. When readiness and eval preflight evidence are required, strict packaging also compares the eval staging manifest hashes against readiness and preflight before writing the ZIP; final audit repeats that check from the returned package. Final audit recomputes each packaged script-copy hash and requires it to match the shell-audit record. The package is refreshed after the terminal packaged manifest stage, so ZIP evidence contains the final stage record.
The `industrial_ai.verify_package` command checks the sidecar checksum, package manifest, every ZIP entry, and any unpacked package/evidence files against the manifest hashes; the full pipeline runs it before exiting, and it can be run again after the artifact is copied back from Leonardo. `industrial_ai.verify_returned_package` wraps ZIP verification and final audit into one returned-artifact check and accepts the same `--count-profile max` shorthand for 150k/family returned-run verification.
The `industrial_ai.final_audit` command ties the returned-run checks together by requiring a passing validation summary, derived run profile consistency, terminal manifest stage, ordered `generation_prepared`/`checkpoint_audited`/`comparisons_complete`/terminal event evidence, run-manifest and package-manifest source-bundle hash agreement with packaged readiness, strict package manifest settings, matching validation/package thresholds, required evidence and submission entries, package-manifest script entries with shell-audit hash matches, packaged CUDA preflight details, packaged corpus audit threshold and family-row evidence, packaged readiness thresholds and eval-required mode, returned-package verification command, objective evidence-report command, resume guidance, dependency-safe split-job commands, readiness launch-command thresholds, source-bundle verification when readiness required it, packaged checkpoint-audit/validation consistency, packaged checkpoint hashes against training summaries, packaged required checkpoint reranker rows and hashes, packaged completion-comparison checkpoint size, recorded validation/package completion-checkpoint-size requirements, packaged reranker pointer consistency, selected reranker score ordering, packaged reranker/completion/inference execution evidence, local/package evidence agreement, and a verified ZIP. It can read validation, manifest, event-log, corpus audit, and execution evidence from unpacked package files or from the ZIP itself, allowing package-only verification after transfer. Its defaults are strict for Leonardo, including CUDA preflight/checkpoint evidence, CUDA comparison/inference transformer-device evidence, selected-checkpoint reranker evidence, generated metadata, readiness evidence, and source-bundle proof; the returned-package wrapper passes `--require-source-bundle-proof` into both final audit and the evidence report, and that proof flag is rejected if readiness is disabled. The full pipeline runs this final audit before exiting when submissions are present and passes the active `COUNT_PER_FAMILY`, reranker-count, completion-comparison-count, and epoch thresholds into it.
The returned-package verifier also runs `industrial_ai.run_evidence_report`, which writes JSON and Markdown summaries that collapse the final objective evidence into one checklist: generated counts, CUDA checkpoint training, packaged model/train-log hash evidence, required checkpoint reranker rows, selected-checkpoint score ordering, medium completion comparison, CUDA inference, ordered run-manifest stages, package manifest, readiness, source-bundle proof, generated launch-command proof, and final audit status. The report prefers evidence inside the returned package over same-named local artifacts by default, with local files used only as fallback. The wrapper then writes `artifacts/returned_package_verification.json`, recording the ZIP/final-audit/report hashes and requiring `objective_ready=true` when the evidence report is enabled.
Successful full-pipeline and finalizer jobs also run `industrial_ai.run_evidence_report` on Leonardo immediately after final audit, so `artifacts/run_evidence_report.json` is available before the package is copied back.
Final Leonardo packaging runs in strict evidence mode, requiring shell audit, readiness, preflight, checkpoint audit, validation, corpus audit, generated metadata, reranker, and `tiny`/`small`/`medium` training evidence before producing the final archive. It also requires the validation summary and checkpoint audit to show the expected derived run profile, and requires the validation summary to show that CUDA preflight validation, eval preflight validation, CUDA checkpoint-device validation, CUDA comparison/inference transformer-device validation, selected-checkpoint reranker validation, generated-corpus minimum and maximum validation, and minimum train-epoch validation were enforced.
It also checks that every required checkpoint size has an available and eligible reranker row whose hash matches the packaged training summary, that the selected checkpoint belongs to a model size whose training evidence was required, that `best_checkpoint.txt` agrees with `metrics.json`, and that the selected reranker row is eligible and tied for the highest eligible selection score.
The validation summary must also show that submissions, generated-data thresholds, reranker-count thresholds, and the required checkpoint sizes were included in validation.
Strict packaging re-hashes the live checkpoint files before writing the ZIP, so a checkpoint replaced after validation is rejected if it no longer matches the training summary, reranker metrics, completion-comparison metrics, or inference summary.

The local `industrial_ai.smoke_pipeline --with-training` command exercises the same strict artifact chain cheaply before submitting the Leonardo job. Add `--generated-per-family 1` to also exercise isolated generated CSVs, generated metadata sidecars, exact generated-corpus validation, strict packaging, final audit, returned-package verification, and run evidence reporting. Add `--require-selected-checkpoint` to mirror the final Leonardo order: compare checkpoint rerankers before inference, require a checkpoint winner, infer with that selected checkpoint, and require selected-checkpoint evidence in validation, packaging, final audit, and returned-package verification. The smoke pipeline enforces local reranker, completion-comparison count, and train-epoch thresholds instead of bypassing them. The no-training smoke mode remains available for a faster package/ZIP sanity check but skips final audit because strict evidence mode requires validation and checkpoint artifacts.

## How To Run

See `README.md` for setup, local smoke run, official eval inference, neural training, and Leonardo Slurm commands.

## What Worked

- The official validator gives a robust anomaly detector for known process-rule violations.
- Long-format sequence data is simple to convert into family-conditioned token sequences.
- A prefix index is a strong completion baseline when generated corpora contain close route variants.
- Larger generated corpora should improve completion because the retrieval/reranking path has more valid suffix candidates.
- Transformer checkpoints can rerank both next-step and completion candidates without replacing the stdlib fallback pipeline.

## What Did Not Work Yet

- Official eval ground truth is not available in this repo, so real final scores cannot be computed locally.
- Transformer checkpoints are not committed yet; they should be trained on Leonardo and referenced here after runs finish.
- The current neural model is intentionally compact and should be tuned with generated data volume, model size, and epoch sweeps.

## Remaining Work Not Claimed In This Submission

- Run the 50k-150k-per-family Leonardo generation and training flow as a real returned package.
- Train and compare `tiny`, `small`, and `medium` checkpoints on CUDA with returned evidence.
- Select and verify a final checkpoint only after returned-package verification passes.
- Add final loss curves, official metrics, and per-family breakdowns when those artifacts exist.

## Credits & Dependencies

- Official challenge data: Lumos Data / Zero One Hack Track 1 Industrial AI.
- Core implementation: Python stdlib baseline and optional PyTorch model.
- Compute target: CINECA Leonardo GPU cluster.
